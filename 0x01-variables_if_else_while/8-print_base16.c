#include <stdio.h>
/**
 * main - prints no.s of base 16 in small letters
 * Return: 0
 * next line
 */
int main(void)
{
	int n;
	char ch;
	
	for(n = 48; n < 58; n++)
	{
		putchar(n);
	}
	fot( ch = 'a'; ch <= 'f'; ch++)
	{
		putchar(n);
	}
	for (ch = 'a'; ch <= 'f'; ch++)
	{
		putchar('\n');
        }
                putchar('\n');
                return (0);
}
