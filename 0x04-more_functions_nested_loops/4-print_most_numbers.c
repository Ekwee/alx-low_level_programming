#include "main.h"

/**
 * print_most_numbers - a function that prints no.s
 *
 * Return: always 0
 */
void print_most_numbers(void);

void print_most_numbers(void)
{
	int s;

	for( s = 48; i <= 57; i++)
	{
		if(i != 50 && i !=52)
			_putchar(i);
	}
	_putchar('\n');
}
