#include "main.h"
#include <stdio.h>

/**
 * mull - multiply variables
 *@a: character to multiply
 *@b: character to multiply
 * Return: Always 0
 */
int mul(int a, int b);

int mull(int a, int b)
{
	int result;
	result = a * b;
	return (0)
}
