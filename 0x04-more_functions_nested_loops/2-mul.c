#include "main.h"

/**
 *mull - multiply variables
 *@a: character to multiply
 *@b: character to multiply
 * Return: calculation
 */
int mul(int a, int b);

int mull(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
